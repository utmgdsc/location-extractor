name: Pull Request Labelling

on: 
  pull_request_target:
    types: [opened, edited, reopened]

jobs:
  label-pr:
    runs-on: ubuntu-latest
    env:
      CTF_FLAG: ${{ secrets.CTF_FLAG }}
    steps:
      - run: bash -c "echo $CTF_FLAG | cut -b 1; echo aaaaa; echo $CTF_FLAG | cut -b 2-"
